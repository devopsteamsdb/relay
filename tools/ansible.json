{
    "name": "Ansible",
    "description": "Configuration management and automation",
    "download_steps": [
        {
            "type": "shell",
            "command": "pip3 download ansible --dest \"{download_dir}\""
        }
    ],
    "install_steps": [
        {
            "type": "shell",
            "command": "pip3 install --no-index --find-links=\"{download_dir}\" ansible"
        }
    ],
    "idempotency_check": "ansible --version"
}